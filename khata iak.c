#include<stdio.h>
int main()
{
    int arr[10],i=0,a,sum[10],p,num[i],n;
    printf("Enter the numbers");
    scanf("The num is %d",&arr[10]);
    if(arr[10]>=0)
        a=arr[10];
    for(i=0;i<10;i++)
    {
        scanf("%d",&sum[i]);

    }
    for(i=0;i<10;i++)
    {
        if(num[10]>=0)
            p=p+num[i];
        else
            n=n+num[i];
    }
}
